#Function that tracks the nergy and the position of the neutrons in the for loop.
#Inputs are a previous theta, phi, r, and energy value that is then converted to
#a new theta, phi, r and energy value
import numpy as np
import math
import random

def neutron(theta, phi, r, energy):
    """This is a function that tracks the energy and position of the simulated neutrons.
    Inputs are a previous array"""




    return new_theta, new_phi, new_r, new_energy

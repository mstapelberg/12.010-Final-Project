import numpy as np
import math

"""The following code is used to sort the neutrons into the right bin to facilitate data analysis"""




def bin_sorter(xneut, yneut, zneut, interaction, energy)
    """This function takes in the position, interaction type, and energy of
    the neutron and sorts it in the correct bin. The bin the neutron is sent
    to is determined by the neutrons position."""


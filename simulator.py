import numpy as np
from finalcodesym import bin_sorter, ni58_total, ni58_ngamma, ni58_nalpha, ni58_elas
import random

seed = 1
def sphere_cart()
def simulator(nparticles, ninteractions, vacradius, vesradius):
    """Simulator that cranks out the Monte Carlo Code in Python"""
    for i in range(nparticles):
    #neutron = neutron_func(i)
        energy = 14E6
        phi = calc_phi()
        theta = calc_theta()
        xneut = 0
        yneut = 0
        zneut = 0
        d = collision_distance(phi, theta, xneut, zneut)
        r = -np.log(random.random(seed))/sigma_t(energy)
        j = 0
        while (j <= ninteractions)
            xneut = sphere_cart(scatter(energy, A)[0:2])

